Review whether the stated goal and acceptance criteria are actually satisfied.

Use concrete tool and test evidence. Treat unsupported completion claims, unfinished todos, failed checks, and changes made after review as failures. End with exactly `VERDICT: PASS` only when completion is proven; otherwise end with `VERDICT: FAIL` and list the smallest required fixes.
